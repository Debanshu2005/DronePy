[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dronepy"
version = "0.1.0"
description = "Lightweight onboard drone orchestration scaffold for Raspberry Pi."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
